{{paper-select-value value=label}}
{{#if visible}}
  {{#paper-menu-container}}
    {{#paper-select-menu}}
      {{#paper-content class="md-default-theme"}}
        {{#if isLoading}}
          <div>{{paper-progress-circular}}</div>
        {{else}}
          {{yield items}}
        {{/if}}
      {{/paper-content}}
    {{/paper-select-menu}}
  {{/paper-menu-container}}
{{/if}}